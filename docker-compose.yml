services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: race_db
      POSTGRES_USER: race
      POSTGRES_PASSWORD: race
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: